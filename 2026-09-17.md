# Sesión 2026-09-17 — Reestructuración de rutas multi-landing

## Contexto

Se reestructuraron las rutas del frontend para que cada landing tenga URLs independientes con su propio formulario y página de gracias, en vez de compartir `/form` y `/gracias`.

## URLs finales (aprobadas por el jefe)

| URL | Página |
|---|---|
| `macondosoftwares.com/vsl/` | Landing VSL |
| `macondosoftwares.com/formvsl/` | Form/Quiz VSL |
| `macondosoftwares.com/graciasvsl/` | Gracias VSL |
| `macondosoftwares.com/lp1/` | Landing LP1 |
| `macondosoftwares.com/lp1/form` | Form/Quiz LP1 |
| `macondosoftwares.com/lp1/gracias` | Gracias LP1 |

`/` redirige a `/vsl`.

## Cambios realizados

### Archivos creados
- `frontend/src/pages/FormVSL.jsx` — formulario propio para VSL (envía `landing: 'vsl-macondo'`)
- `frontend/src/pages/FormLp1.jsx` — formulario propio para LP1 (envía `landing: 'lp1'`)
- `frontend/src/pages/GraciasVSL.jsx` — página de gracias propia para VSL
- `frontend/src/pages/GraciasLp1.jsx` — página de gracias propia para LP1

### Archivos eliminados
- `frontend/src/pages/FormPage.jsx` — reemplazado por FormVSL y FormLp1
- `frontend/src/pages/Gracias.jsx` — reemplazado por GraciasVSL y GraciasLp1

### Archivos modificados
- `frontend/src/App.jsx` — nuevas rutas, redirect `/` → `/vsl`, import actualizados, LANDINGS actualizado
- `frontend/src/pages/LandingVSL.jsx` — navigate a `/formvsl`
- `frontend/src/pages/LandingLp1.jsx` — navigate a `/lp1/form`
- `frontend/src/pages/FormVSL.jsx` — navigate a `/graciasvsl`, pixels agregados
- `frontend/src/pages/FormLp1.jsx` — navigate a `/lp1/gracias`, pixels agregados
- `plan-proyecto-vsl.md` — sección 1.1 con mapa de URLs finales

### Backend
- Sin cambios. El generador UTM (`utm.controller.js`) ya tenía las rutas correctas (`/vsl` y `/lp1`). CORS se configura por dominio, no por ruta.

### Pixels
- Meta Pixel + LinkedIn Insight Tag cargados en las 6 URLs públicas
- Se agregaron a FormVSL.jsx y FormLp1.jsx (no los tenían)
- No se cargan en `/crm`

## Decisión de diseño

Cada landing tiene archivos separados de form y gracias (no comparten componente) para permitir personalización independiente: diferentes preguntas de quiz, diseño, textos y flujo. La lógica interna es idéntica hoy pero puede divergir en el futuro sin afectar la otra landing.

## Verificación

- Build exitoso (`vite build`): 86 módulos, 0 errores, 0 warnings
